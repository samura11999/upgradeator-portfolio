{
  "name": "upgradeator-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Static portfolio app for choosing a PC GPU upgrade.",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "start": "npx serve ."
  }
}